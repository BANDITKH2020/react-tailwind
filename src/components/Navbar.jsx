const Navbar = () => {
    return (
        <nav>
            <div>
                <div>
                    <a href="/">Logo</a>
                </div>
            </div>
        </nav>
    );
};

export default Navbar;

